{
  "name": "vite-rolldown-benchmark-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:clean": "rm -rf ../../../dist/vite-rolldown node_modules/.vite && vite build"
  },
  "devDependencies": {
    "vite": "8.0.0-beta.8"
  }
}

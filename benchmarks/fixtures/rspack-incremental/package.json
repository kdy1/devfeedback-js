{
  "name": "rspack-incremental-benchmark-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rspack build",
    "build:clean": "rm -rf ../../dist/rspack-incremental node_modules/.cache && rspack build"
  }
}

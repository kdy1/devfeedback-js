{
  "name": "rsbuild-optimized-benchmark-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "build:clean": "rm -rf ../../dist/rsbuild-optimized node_modules/.cache && rsbuild build"
  }
}

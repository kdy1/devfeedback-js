{
  "name": "rspack-benchmark-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rspack build",
    "build:clean": "rm -rf ../../dist/rspack-app node_modules/.cache && rspack build"
  }
}
